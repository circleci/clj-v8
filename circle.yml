machine:
  java:
    version: oraclejdk8

dependencies:
  override:
    - git submodule update --init --depth 1
    - cd clj-v8 && lein deps

test:
  override:
    - cd clj-v8 && lein test
    - cd clj-v8-native && ./build.sh
